# 任务模板配置与运行闭环

## 使用路径

自动化与任务 → 模板配置 → 新建模板：

1. 基本信息：名称、说明。
2. 输入参数：选择已注册的输入 Skill，在沙箱表单中设置并应用默认参数。
3. 执行能力：选择已注册的执行 Skill。
4. 结果与展示：选择结果处理与展示 Skill，配置 JSON / HTML / CSV 导出、报告标题、主题色、摘要、明细和默认页签。
5. 试运行与发布：先保存并试运行，检查报告预览，再发布当前版本。

发布后，在“模板任务”选择该模板创建任务。参数会预填，仍可修改。运行后可以查询报告、结构化数据、输入快照，下载已启用的格式。

内置经营汇总模板继续可直接使用。默认参数是固定值，不会自动将月份推进到当前月份。

## 生命周期与数据归属

- 自定义模板以当前组织和账号隔离保存到 API 的 domain KV，不使用浏览器本地存储作为事实源。
- 草稿支持保存、继续编辑和复制；修改采用 revision 校验，避免覆盖他人或其他页面的新修改。
- 试运行校验 Skill 入口、契约兼容性、默认参数、执行输出、报告和导出能力。当前注册能力是无外部副作用的经营汇总 Skill；试运行不创建正式任务或运行结果。
- 验证绑定草稿 revision，任何保存修改都会使旧验证失效。只有当前 revision 通过试运行，才允许发布。
- 发布版本从 1.0.0 递增；发布版本不可覆盖。编辑已发布模板生成下一草稿版本，旧任务继续绑定旧版本。
- 停用模板从新建列表隐藏并阻止新建；已有任务仍可编辑参数、执行和查询历史。支持恢复。
- 每次运行保存模板、输入、参数版本及结果展示/导出配置快照，历史结果不跟随模板变更。

## 实现位置

- `packages/contracts/src/task-templates.ts`：模板、配置、版本记录、能力声明、任务和结果契约。
- `packages/orchestrator/src/template-tasks.ts`：模板生命周期与任务运行服务。
- `packages/orchestrator/src/task-skills/business-summary`：SKILL.md、能力描述、输入 HTML、执行和格式化入口、报告及 CSV 导出。
- `apps/api/src/modules/tasks/routes.ts`：认证后的配置、试运行、发布、任务、查询与下载接口。
- `TaskTemplateManager.vue`：五步模板配置界面。
- `TemplateTasksPanel.vue`：任务创建、修改、运行与历史结果展示。

## 运行与安全约束

- iframe 输入页仅允许脚本，不允许同源和网络，通过 source + 每次编辑 channel 校验消息；界面不持有凭据。用户应用参数后由宿主提交，服务端再次校验。
- 结果 HTML 转义用户内容，报告在无脚本沙箱中展示；CSV 转义单元格并处理业务名称中的公式前缀。
- 金额按分计算，零收入利润率为 null；利润仅为用户输入收入减成本，不代表净利润。
- 服务端验证导出格式策略。隐藏报告明细仅影响展示，不构成数据访问权限限制。
- 请求 ID 避免重试重复执行；服务重启将中断运行标记失败，不自动重放。
- 不兼容的展示模板回退结构化数据。

## 当前范围

配置已经完整接入保存、验证、发布和运行，不是只保存表单的原型。但能力目录目前只有内置经营数据汇总 Skill，输入最多 100 行，结果内联保存于运行记录。

已落地一个可操作的数据库取数闭环：数据工作台和“自动化与任务”均提供“数据库任务闭环体验”入口；首次点击会在当前账号的默认工作库创建 4 条幂等测试记录，客户下拉选项由 `/data/gateway/options` 从该表查询，运行时服务端按当前账号所有权校验读取数据，调用经营汇总 Skill，并将报告与结构化结果写回一个新的“测试结果”数据源。重复初始化不会重复建表；运行请求用 requestId 去重；其他账号不能读取。当前 demo 的字段映射固定为客户编号/客户名称/月份/收入/成本，作为下一步抽象可配置数据能力的参考实现。

尚未支持任意上传执行代码、动态安装 Skill、可视化字段 schema 设计、任意数据库/API 连接配置、外部业务写入、智能体执行分支及新模板定时调度。原有定时任务单独保留。后续新增能力需实现执行入口、数据契约与能力描述，并显式注册；有副作用能力接入前需要独立的试运行/交付机制。

## 验证

```sh
pnpm --filter @workmate/contracts build
pnpm --filter @workmate/orchestrator test
pnpm --filter @workmate/api typecheck
pnpm --filter @workmate/api build
pnpm --filter @workmate/renderer build
pnpm tasks:smoke
```

可选浏览器回归：安装 Playwright 后运行 `node scripts/template-tasks-browser-smoke.cjs`；也可通过 `WORKMATE_PLAYWRIGHT_MODULE` 指定已有模块路径，通过 `WORKMATE_BROWSER_CHANNEL=chrome` 使用 Chrome。需先完成 API 与 renderer 构建。

回归使用临时数据目录与独立账号，覆盖数据库测试数据初始化、客户查询、月份筛选、并发重试去重、结果数据集回存、账号隔离，以及模板配置、试跑发布、默认参数、定制报告、CSV 下载和旧任务流程，截图保存于系统临时目录，不修改真实业务数据。
